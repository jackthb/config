---
description: Compose a message and copy to clipboard
allowed-tools: Bash(pbcopy:*)
---

Based on the conversation context, compose a message for:

$ARGUMENTS

After composing, copy the final message text to the clipboard using pbcopy. Only copy the message itself, not any explanation or commentary.
